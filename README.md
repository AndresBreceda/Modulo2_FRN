# Importante
* Reglas para subir tu proyecto

A la hora de subir tu proyecto, solo sube la carpeta del mismo, cada carpeta debe ser un proyecto separado, de manera que cada uno funcione por si mismo, deberia tener core, vistas, manage.py y su propio contenedor de docker, en esta etapa del proyecto no subas archivos solos fuera de la carpeta de tu mismo proyecto